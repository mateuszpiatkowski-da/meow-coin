# Improvements

## Token Standard API

1. make splice/token-standard a publishable package - will ease up the process of importing `.dar` files
2. Bundle `.dar` and `.yaml`/generated `.ts` files together
